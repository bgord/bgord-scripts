pre-commit:
  commands:
    readme-generate-package:
      run: ./bgord-scripts/readme-generate-package.sh
      stage_fixed: true
pre-push:
  parallel: true
  commands:
    check-package-config:
      run: ./bgord-scripts/check-package-config.sh
