generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./sqlite.db"
}

model Example {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
}
